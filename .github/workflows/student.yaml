name: studentregstationciworkflw
on: 
    workflow_dispatch
jobs:
    git_Checkout:
        runs-on: ubuntu-latest
        steps:
          - name: build command
            run: echo "print srinu"
    test:
       runs-on: ubuntu-latest
       steps:
          - name: testing a package
            run: echo "print test"
            
             





  
