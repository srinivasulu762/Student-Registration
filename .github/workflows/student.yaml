name: studentregstationciworkflw
on: 
    workflow_dispatch
jobs:
    git_Checkout:
        runs-on: ubuntu-latest
        steps:
          - name: build command
            run: echo "print ${{ vars.NAME }}"
    test:
       runs-on: ubuntu-latest
       steps:
          - name: testing a package
            run: echo "print ${{ vars.NAME }}."
    secrets:
       runs-on: ubuntu-latest
       steps:
        - name: adding secrets 
          run: echo "print token ${{ secrets.TOKEN }}"
            
             





  
